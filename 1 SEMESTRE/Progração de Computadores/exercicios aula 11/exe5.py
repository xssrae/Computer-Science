letras = []
texto = input('Digite um texto:').upper()
print(texto)

for letra in texto:
    existe = 0
    for i in letras:
        if letras:
            if letras == i:
        if existe == 0:
            letras.append(letra)
            q = texto.

    q = texto.count(letra)
    print(q)

